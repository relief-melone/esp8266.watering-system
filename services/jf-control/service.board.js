const connect = require('./board/service.board.connect');
const initialize = require('./board/service.board.initialize');
const getBoard = require('./board/service.board.getBoard');

module.exports = {
    initialize,
    connect,
    getBoard
}
